# Serve static /install pages; SPA fallback for the app shell only.
/install/*
  /install/:splat
  200

/install
  /install/index.html
  200

# /privacy-policy is handled by the SPA (main.tsx path switch).
/*
  /index.html
  200
